export function HelloWorldArticle() {
  return (
    <article>
      <h1>A more Complex Component</h1>
      <p>
        This exercise lets you build a React component containing more than one
        HTML tag. Have a look at the [App.js](./src/App.js) file: the default
        export function called `App` returns a div which wants to be replaced.
        Let us comply with its request!
      </p>
    </article>
  );
}
